# Instructions
You need `nodejs >= v24.12.0`

Install dependencies with (one time):
```bash
npm install
```

Create a database (one time):
```bash
npm run seed
```

Start the server:
```bash
npm run dev
```